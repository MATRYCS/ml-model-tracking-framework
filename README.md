# ML Model Tracking Framework

## Project structure

- `./mlops` directory contains docker compose instructions to build local deployment.
- `./examples` directory contains code samples of using MLFLow.

